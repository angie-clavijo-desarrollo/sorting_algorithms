#include "sort.h"


void insertion_sort_list(listint_t **list)
{
    listint_t *tmp = NULL, *aux = NULL, *node_next = NULL;

    if (aux->n > aux->next->n)

        tmp = (aux->next->n);
        node_next->n = aux->n;
        aux->n = tmp;
        aux = aux->next;
    else;
        aux = aux->next


}
